# Copy this to secrets.toml for local use, OR paste these lines into the
# "Secrets" panel in Streamlit Community Cloud (Settings → Secrets).
#
# DO NOT commit secrets.toml — it's in .gitignore.

ANTHROPIC_API_KEY = "sk-ant-paste-your-real-key-here"
